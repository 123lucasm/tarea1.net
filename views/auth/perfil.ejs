<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - UTEC</title>
    
    <!-- Tailwind CSS Local -->
    <link rel="stylesheet" href="/css/tailwind.css">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- AOS Animations -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Custom CSS for Index Page like Perfil Navigation -->
    <link rel="stylesheet" href="/css/index.css">
    
    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>

<body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">
                <i class="fas fa-graduation-cap me-2"></i>
                <strong>MATRICULATEC</strong>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-togg-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto" style="margin-right: 1rem;">
                    <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-user me-1"></i>
                                <%= usuario.nombre %>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/dashboard">Mi Dashboard</a></li>
                                <li><a class="dropdown-item" href="/auth/perfil">Mi Perfil</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/auth/logout">Cerrar Sesión</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">Iniciar Sesión</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/registro">Registrarse</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content Container -->
    <div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100" style="padding-top: 80px;">
        <div class="container mx-auto px-4 py-8">
    <!-- Header Section -->
            <div class="bg-white rounded-2xl shadow-xl bg-gradient-to-r from-white to-blue-50 border border-blue-100 mb-8">
                <div class="px-8 py-6">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-4">
                            <div class="w-16 h-16 bg-gradient-to-r from-blue-600 to-indigo-600 rounded-xl flex items-center justify-center shadow-lg">
                                <i class="fas fa-user-circle text-2xl text-white"></i>
                            </div>
            <div>
                                <h1 class="text-3xl font-bold bg-gradient-to-r from-gray-900 to-blue-800 bg-clip-text text-transparent">
                    Mi Perfil
                </h1>
                                <p class="text-gray-600 mt-1">
                    Gestiona tu información personal y configuración de cuenta
                </p>
            </div>
                        </div>
                        <% if (typeof actualizacionObligatoria === 'undefined' || !actualizacionObligatoria) { %>
                        <button id="edit-profile-btn" class="bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white px-6 py-3 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105">
                            <i class="fas fa-edit mr-2"></i>
                Editar Perfil
            </button>
                        <% } %>
        </div>
    </div>

                <!-- Alerta de actualización obligatoria si aplica -->
                <% if (typeof actualizacionObligatoria !== 'undefined' && actualizacionObligatoria) { %>
                <div class="bg-gradient-to-r from-yellow-400 to-orange-400 border-l-4 border-yellow-600 rounded-lg m-8" role="alert">
                    <div class="flex items-center p-4">
                        <i class="fas fa-exclamation-triangle text-2xl text-yellow-800 mr-4"></i>
                        <div>
                            <div class="text-lg font-bold text-yellow-800">Actualización obligatoria requerida:</div>
                            <div class="text-yellow-700 mt-1">
                                <% if (primeraVezGoogle) { %>
                                Como primer usuario con Google, necesitas completar tu perfil estableciendo tu cédula real. No puedes continuar hasta que actualices esta información.
                                <% } else { %>
                                Necesitas actualizar tu información de perfil antes de continuar. Por favor, completa los campos requeridos.
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
            </div>

            <!-- Layout Grid -->
            <div class="grid grid-cols-1 lg:grid-cols-4 gap-8">
        <!-- Sidebar de Navegación del Perfil -->
                <div class="lg:col-span-1">
                    <div class="bg-white rounded-2xl shadow-xl border border-gray-200 overflow-hidden">
                <!-- Información del Usuario -->
                        <div class="bg-gradient-to-r from-blue-600 to-indigo-600 p-6 text-center">
                            <div class="w-24 h-24 bg-white rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
                                <span class="text-3xl font-bold text-blue-600" id="sidebar-initials">
                                    <%= usuario ? usuario.nombre.charAt(0).toUpperCase() + usuario.apellido.charAt(0).toUpperCase() : 'U' %>
                                </span>
                            </div>
                            <h3 class="text-xl font-bold text-white mb-1" id="sidebar-name">
                            <%= usuario ? usuario.nombre + ' ' + usuario.apellido : 'Usuario' %>
                        </h3>
                            <p class="text-blue-100 text-sm mb-3" id="sidebar-email">
                            <%= usuario ? usuario.email : 'usuario@ejemplo.com' %>
                        </p>
                            <span class="inline-block bg-yellow-400 text-yellow-900 px-3 py-1 rounded-full text-xs font-semibold" id="sidebar-role">
                            <%= usuario ? (usuario.rol === 'estudiante' ? 'Estudiante' : 'Administrador') : 'Usuario' %>
                        </span>
                </div>

                <!-- Menú de Navegación -->
                        <div class="p-4">
                            <h4 class="text-sm font-semibold text-gray-500 mb-4 uppercase tracking-wider">Menú de Perfil</h4>
                            <nav class="space-y-2">
                                <a href="#informacion-personal" class="flex items-center px-4 py-3 rounded-xl text-gray-700 hover:bg-blue-50 hover:text-blue-700 transition-all duration-300 profile-menu-item active" data-section="informacion-personal">
                                    <i class="fas fa-user mr-3 text-blue-500"></i>
                            Información Personal
                        </a>
                                <a href="#seguridad" class="flex items-center px-4 py-3 rounded-xl text-gray-700 hover:bg-blue-50 hover:text-blue-700 transition-all duration-300 profile-menu-item" data-section="seguridad">
                                    <i class="fas fa-shield-alt mr-3 text-blue-500"></i>
                            Seguridad
                        </a>
                    </nav>
                </div>
            </div>
        </div>

        <!-- Contenido Principal -->
        <div class="lg:col-span-3">
            <!-- Sección: Información Personal -->
            <div id="informacion-personal" class="profile-section">
                <div class="bg-white rounded-2xl shadow-xl border border-gray-200 overflow-hidden">
                    <div class="bg-gradient-to-r from-blue-600 to-indigo-600 px-8 py-6">
                        <h3 class="text-2xl font-bold text-white flex items-center">
                            <i class="fas fa-user mr-3"></i>
                            Información Personal
                        </h3>
                        <p class="text-blue-100 mt-1">Gestión de datos personales y contacto</p>
                    </div>

                    <form id="profile-form" class="p-8">
                            <!-- Información Básica -->
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">
                                    <i class="fas fa-user mr-2 text-blue-500"></i>
                                    Nombre
                                </label>
                                <input type="text" 
                                        id="nombre" 
                                        name="nombre" 
                                        class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 bg-gray-50" 
                                        value="<%= usuario ? usuario.nombre : '' %>" 
                                        readonly>
                                </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">
                                    <i class="fas fa-user mr-2 text-blue-500"></i>
                                    Apellido
                                </label>
                                <input type="text" 
                                        id="apellido" 
                                        name="apellido" 
                                        class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 bg-gray-50" 
                                        value="<%= usuario ? usuario.apellido : '' %>" 
                                        readonly>
                            </div>
                                </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">
                                    <i class="fas fa-envelope mr-2 text-blue-500"></i>
                                    Email
                                </label>
                                <input type="email" 
                                        id="email" 
                                        name="email" 
                                        class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 bg-gray-50" 
                                        value="<%= usuario ? usuario.email : '' %>" 
                                        readonly>
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">
                                    <i class="fas fa-id-card mr-2 text-blue-500"></i>
                                    Cédula
                                    <% if (typeof actualizacionObligatoria !== 'undefined' && actualizacionObligatoria) { %>
                                    <span class="text-red-500 ml-1">*</span>
                                    <% } %>
                                </label>
                                <input type="text" 
                                        id="cedula" 
                                        name="cedula"
                                        class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 <% if (typeof actualizacionObligatoria === 'undefined' || !actualizacionObligatoria) { %>bg-gray-50<% } %>"
                                        value="<%= usuario ? (usuario.cedula || '') : '' %>"
                                        <% if (typeof actualizacionObligatoria === 'undefined' || !actualizacionObligatoria) { %>readonly<% } %>
                                        <% if (typeof actualizacionObligatoria !== 'undefined' && actualizacionObligatoria) { %>required<% } %>
                                        placeholder="<% if (typeof actualizacionObligatoria !== 'undefined' && actualizacionObligatoria) { %>Ingresa tu cédula real<% } %>"
                                >
                                <% if (typeof actualizacionObligatoria !== 'undefined' && actualizacionObligatoria) { %>
                                <small class="text-gray-500 mt-1 block">Campo obligatorio para usuarios nuevos con Google</small>
                                <% } %>
                            </div>
                        </div>

                        <!-- Información Adicional -->
                        <div class="mb-6">
                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                <i class="fas fa-file-text mr-2 text-blue-500"></i>
                                Biografía
                            </label>
                            <textarea id="biografia" 
                                        name="biografia" 
                                        class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 bg-gray-50" 
                                        rows="4" 
                                        readonly
                            ><%= usuario ? (usuario.biografia || '') : '' %></textarea>
                            </div>

                            <!-- Configuración de Cuenta -->
                        <div class="border-t border-gray-200 pt-8 mb-6">
                            <div class="bg-gray-50 rounded-xl p-6">
                                <h4 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                                    <i class="fas fa-cog mr-2 text-blue-500"></i>
                                    Configuración de Cuenta
                                </h4>
                                
                                <div class="space-y-4">
                                    <div class="flex items-center justify-between p-4 bg-white rounded-lg border border-gray-200">
                                        <div>
                                            <h5 class="font-semibold text-gray-900">Notificaciones por Email</h5>
                                            <p class="text-sm text-gray-600">Recibir notificaciones importantes por correo electrónico</p>
                                        </div>
                                        <label class="relative inline-flex items-center cursor-pointer">
                                            <input type="checkbox" id="notificaciones-email" class="sr-only peer" <%= usuario && usuario.notificacionesEmail ? 'checked' : '' %>>
                                            <div class="w-11 h-6 bg-gray-300 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-blue-500"></div>
                                        </label>
                                    </div>

                                    <div class="flex items-center justify-between p-4 bg-white rounded-lg border border-gray-200">
                                        <div>
                                            <h5 class="font-semibold text-gray-900">Modo Oscuro</h5>
                                            <p class="text-sm text-gray-600">Cambiar el tema de la interfaz a modo oscuro</p>
                                        </div>
                                        <label class="relative inline-flex items-center cursor-pointer">
                                            <input type="checkbox" id="modo-oscuro" class="sr-only peer" <%= usuario && usuario.modoOscuro ? 'checked' : '' %>>
                                            <div class="w-11 h-6 bg-gray-300 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-blue-500"></div>
                                        </label>
                                    </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Botones de Acción -->
                        <div class="flex justify-end gap-4 pt-6 border-t border-gray-200">
                            <% if (typeof actualizacionObligatoria === 'undefined' || !actualizacionObligatoria) { %>
                            <button type="button" id="cancel-edit-btn" class="px-6 py-3 text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-xl font-semibold transition-all duration-300" style="display: none;">
                                    <i class="fas fa-times mr-2"></i>
                                    Cancelar
                                </button>
                            <button type="submit" id="save-profile-btn" class="px-6 py-3 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300" style="display: none;">
                                    <i class="fas fa-save mr-2"></i>
                                    Guardar Cambios
                                </button>
                            <% } else { %>
                            <button type="submit" id="save-profile-btn" class="px-6 py-3 bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105">
                                <i class="fas fa-save mr-2"></i>
                                Guardar y Continuar
                            </button>
                            <% } %>
                            </div>
                        </form>
                    </div>
                </div>


            <!-- Sección: Seguridad -->
            <div id="seguridad" class="profile-section bg-white rounded-2xl shadow-xl border border-gray-200" style="display: none;">
                <div class="bg-gradient-to-r from-red-600 to-red-700 px-8 py-6">
                    <h3 class="text-2xl font-bold text-white flex items-center">
                        <i class="fas fa-shield-alt mr-3"></i>
                            Seguridad
                        </h3>
                    <p class="text-red-100 mt-1">Protege tu cuenta con configuración de seguridad</p>
                </div>
                        
                <div class="p-8">
                    <div class="bg-gradient-to-r from-red-50 to-orange-50 rounded-xl border border-red-200 mb-6">
                        <div class="flex items-center justify-between p-6">
                            <div>
                                <h5 class="font-semibold text-gray-900">Cambiar Contraseña</h5>
                                <p class="text-sm text-gray-600">Actualiza tu contraseña para mantener tu cuenta segura</p>
                            </div>
                            <button id="change-password-btn" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-xl font-semibold transition-all duration-300 shadow-lg">
                                <i class="fas fa-key mr-2"></i>
                                Cambiar Contraseña
                            </button>
                        </div>
                        </div>

                    <form id="password-form" class="space-y-6 bg-gray-50 rounded-xl p-6" style="display: none;">
                            <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                <i class="fas fa-lock mr-2 text-blue-500"></i>
                                Contraseña Actual
                            </label>
                                <div class="relative">
                                <input type="password" id="current-password" name="currentPassword" class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 pr-12">
                                <button type="button" class="absolute inset-y-0 right-0 pr-4 flex items-center" onclick="togglePassword('current-password')">
                                    <i class="fas fa-eye text-gray-400 hover:text-gray-600"></i>
                                    </button>
                                </div>
                            </div>

                            <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                <i class="fas fa-key mr-2 text-green-500"></i>
                                Nueva Contraseña
                            </label>
                                <div class="relative">
                                <input type="password" id="new-password" name="newPassword" class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 pr-12">
                                <button type="button" class="absolute inset-y-0 right-0 pr-4 flex items-center" onclick="togglePassword('new-password')">
                                    <i class="fas fa-eye text-gray-400 hover:text-gray-600"></i>
                                    </button>
                                </div>
                            <div class="mt-3 p-4 bg-white rounded-lg border">
                                <p class="text-sm text-gray-600 mb-2">La contraseña debe tener al menos:</p>
                                <ul class="text-sm space-y-1">
                                    <li id="length-check" class="password-check text-red-500 flex items-center"><span class="mr-2">⭕</span>8 caracteres</li>
                                    <li id="uppercase-check" class="password-check text-red-500 flex items-center"><span class="mr-2">⭕</span>1 letra mayúscula</li>
                                    <li id="lowercase-check" class="password-check text-red-500 flex items-center"><span class="mr-2">⭕</span>1 letra minúscula</li>
                                    <li id="number-check" class="password-check text-red-500 flex items-center"><span class="mr-2">⭕</span>1 número</li>
                                        </ul>
                                </div>
                            </div>

                            <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">
                                <i class="fas fa-check-circle mr-2 text-blue-500"></i>
                                Confirmar Nueva Contraseña
                            </label>
                                <div class="relative">
                                <input type="password" id="confirm-password" name="confirmPassword" class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all duration-300 pr-12">
                                <button type="button" class="absolute inset-y-0 right-0 pr-4 flex items-center" onclick="togglePassword('confirm-password')">
                                    <i class="fas fa-eye text-gray-400 hover:text-gray-600"></i>
                                    </button>
                                </div>
                            </div>

                        <div class="flex justify-end gap-4 pt-4 border-t border-gray-200">
                            <button type="button" id="cancel-password-btn" class="px-6 py-3 text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-xl font-semibold transition-all duration-300">
                                    <i class="fas fa-times mr-2"></i>
                                    Cancelar
                                </button>
                            <button type="submit" id="save-password-btn" class="px-6 py-3 bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300">
                                    <i class="fas fa-save mr-2"></i>
                                    Cambiar Contraseña
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gradient-to-r from-blue-900 via-blue-800 to-blue-700 text-white py-8 mt-16">
        <div class="container mx-auto px-4 text-center">
            <div class="flex items-center justify-center mb-4">
                <i class="fas fa-graduation-cap text-yellow-400 text-2xl mr-3"></i>
                <span class="text-xl font-bold">MATRICULATEC</span>
</div>
            <p class="text-blue-100">
                Sistema de Gestión Académica Inteligente de UTEC
            </p>
            <p class="text-blue-200 mt-2 text-sm">
                &copy; 2024 UTEC - Universidad Tecnológica del Uruguay. Todos los derechos reservados.
            </p>
</div>
    </footer>

    <!-- External Files JS -->
    <!-- AOS -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Main Scripts -->
    <script src="/js/perfil-manage.js"></script>
    <script src="/js/perfil-main.js"></script>
</body>
</html>